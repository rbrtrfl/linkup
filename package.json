{
  "name": "linkup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --project ./server",
    "start": "node ./server/dist/index.js",
    "postinstall": "cd client && npm i && cd ../server && npm i && tsc",
    "deploy": "cd client && npm run build && cd .. && git add . && git commit -m Heroku && git push heroku heroku:main"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rbrtrfl/linkup.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rbrtrfl/linkup/issues"
  },
  "homepage": "https://github.com/rbrtrfl/linkup#readme",
  "devDependencies": {
    "cz-conventional-changelog": "^3.3.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "dependencies": {
    "redux-devtools-extension": "^2.13.9"
  },
  "engines": {
    "node": "17.2.0"
  },
  "prisma": {
    "schema": "./server/prisma/schema.prisma"
  }
}
